CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  fname VARCHAR(50),
  lname VARCHAR(50),
  address TEXT,
  email VARCHAR(100) UNIQUE,
  password VARCHAR(100),
  phone VARCHAR(15),
  role ENUM('buyer', 'seller')
);